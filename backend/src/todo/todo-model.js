import client from "../db/db";

const todoModel = {
    create: async (id, text) => {

    },
    read: async (id) => {

    },
    readAll: async () => {

    },
    update: async (id, { text, done }) => {

    },
    delete: async (id) => {

    }
}

export default todoModel